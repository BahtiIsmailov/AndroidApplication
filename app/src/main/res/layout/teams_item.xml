<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto">


    <ImageView
        android:id="@+id/teams_image"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_marginStart="16dp"
        android:layout_marginTop="16dp"
        android:src="@drawable/ic_team_house"
        />
    <TextView
        android:id="@+id/teams_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toEndOf="@+id/teams_image"
        app:layout_constraintTop_toTopOf="@+id/teams_image"
        app:layout_constraintBottom_toBottomOf="@+id/teams_image"
        android:layout_marginStart="16dp"
        android:textColor="@color/black"
        android:textSize="17sp"
        android:text="adsasdasdaasd"
        />
    <ImageView
        android:id="@+id/teams_checking"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@+id/teams_image"
        app:layout_constraintBottom_toBottomOf="@+id/teams_image"
        android:layout_marginEnd="16dp"
        android:src="@drawable/ic_teams_unheck"
        />

</androidx.constraintlayout.widget.ConstraintLayout>